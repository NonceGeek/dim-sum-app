This folder is created to save all the scripts about the data.

Basically, the script is coding by the `typescript/javascript`.

There are some guides:

* if handling `.csv`, use '@vanillaes/csv' to parse the `.csv` file.