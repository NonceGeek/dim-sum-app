# Repository workflow

- 未经用户明确批准，禁止创建、调用或委派 Subagent。
- 本仓库发布流程统一为：先提交并推送到 `dev`，再创建 `dev → main` 的 PR。不得默认绕过 `dev` 从功能分支直接发布到 `main`。
- 开始发布前先 fetch 并核对 `origin/dev` 与 `origin/main`。如果 `dev` 仅落后，先快进同步；如存在双方独有提交，先审查并合并，禁止 force push 覆盖历史。
- PR 合入 `main` 后，再将 `main` 的合并提交快进同步回 `dev`，核验远程分支内容和历史一致。
- 工作区存在未提交内容时，保留用户改动；可使用独立工作副本操作 `dev`，不要通过 reset、clean 或覆盖文件来清理原工作区。
- 代码上线、数据库迁移、外部平台配置、定时任务和真实业务联调分别记录状态；不能把“代码已部署”描述为“业务流程已启用”。
